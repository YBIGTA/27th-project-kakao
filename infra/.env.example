
# =============================================================================
# 카카오톡 선물 추천 시스템 환경 변수 설정
# =============================================================================

# =============================================================================
# 데이터베이스 설정
# =============================================================================
# PostgreSQL 연결 URL 
DB_URL=postgresql://username:password@localhost:5432/gift_recommender

# 데이터베이스 연결 풀 설정
DB_POOL_MIN=1
DB_POOL_MAX=10
PG_STMT_TIMEOUT_MS=5000

# =============================================================================
# GPU 서비스 설정 (RunPod)
# =============================================================================
# GPU 엔드포인트 URL 
GPU_ENDPOINT_URL=https://your-endpoint-id.proxy.runpod.net

# RunPod API 키 
RUNPOD_API_KEY=your_runpod_api_key_here

# =============================================================================
# LLM Provider 설정
# =============================================================================
# LLM_PROVIDER=openai 또는 upstage
LLM_PROVIDER=upstage
LLM_MODEL=solar-1-mini-chat

# OpenAI API 설정
OPENAI_API_KEY=your_openai_api_key_here

# 업스테이지 API 설정 (50달러 크레딧 활용)
UPSTAGE_API_KEY=your_upstage_api_key_here

# =============================================================================
# 알고리즘 가중치 설정
# =============================================================================
# 시간 가중치 (일)
RECENCY_HALF_LIFE_DAYS=30   # 최근성 반감기

# 카테고리 선택 설정
MAX_CATEGORIES=3            # 최대 카테고리 수
MIN_CAT_SCORE=0.10          # 최소 카테고리 점수

# =============================================================================
# 개발/디버깅 설정
# =============================================================================
# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 개발 모드 (true/false)
DEBUG=false

# =============================================================================
# 서버 설정
# =============================================================================
# 서버 호스트 (기본값: 0.0.0.0)
HOST=0.0.0.0

# 서버 포트 (기본값: 8000)
PORT=8000

# =============================================================================
# 보안 설정
# =============================================================================
# CORS 허용 도메인 
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

